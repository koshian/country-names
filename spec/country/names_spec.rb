require 'spec_helper'

describe Country::Names do
  it 'has a version number' do
    expect(Country::Names::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
